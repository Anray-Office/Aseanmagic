{% comment %}
  Renders Aseanmagic custom menu for the header.

  Usage:
  {% render 'Aseanmagic-header-menu' %}
{% endcomment %}

<nav class="header__inline-menu aseanmagic-menu-desktop">
    <div class="header__logo-time-container">
      {% assign am_left_logo_w = 1347 %}
      {% assign am_left_logo_h = 456 %}
      {% assign am_right_logo_w = 2048 %}
      {% assign am_right_logo_h = 240 %}
      <div class="am-promo-bar">
        <div class="am-col am-col--left">
          <a href="{{ routes.root_url }}" class="am-left-fixed">
            <img class="am-left-fixed__img" src="https://cdn.shopify.com/s/files/1/0924/6244/9007/files/CA_Logo_without2012.jpg?v=1755761735" alt="COSMOPROF ASIA" loading="lazy" width="{{ am_left_logo_w }}" height="{{ am_left_logo_h }}">
          </a>
        </div>

        <div class="am-col am-col--center">
          <p class="am-center__line">第28届</p>
          <p class="am-center__line">中国香港</p>
        </div>

        <div class="am-col am-col--right am-col--r1">
          <div class="am-right-item">
            <h5 class="am-right-item__title">2025年11月11-13日</h5>
            <p class="am-right-item__subtitle">亚洲国际博览馆</p>
            <div class="am-logos">
              <img class="am-logo-img" src="https://cdn.shopify.com/s/files/1/0924/6244/9007/files/CPKA_Machinery_nero_rgb-2048x240.jpg?v=1755761739" alt="COSMOPACK ASIA" loading="lazy" width="{{ am_right_logo_w }}" height="{{ am_right_logo_h }}">
              <img class="am-logo-img" src="https://cdn.shopify.com/s/files/1/0924/6244/9007/files/CPKA_Machinery_nero_rgb-2048x240.jpg?v=1755761739" alt="COSMOPACK ASIA" loading="lazy" width="{{ am_right_logo_w }}" height="{{ am_right_logo_h }}">
            </div>
          </div>
        </div>

        <div class="am-col am-col--right am-col--r2">
          <div class="am-right-item">
            <h5 class="am-right-item__title">2025年11月12-14日</h5>
            <p class="am-right-item__subtitle">香港会议展览中心</p>
            <div class="am-logos">
              <img class="am-logo-img" src="https://cdn.shopify.com/s/files/1/0924/6244/9007/files/CPKA_Machinery_nero_rgb-2048x240.jpg?v=1755761739" alt="COSMOPROF ASIA" loading="lazy" width="{{ am_right_logo_w }}" height="{{ am_right_logo_h }}">
              <img class="am-logo-img" src="https://cdn.shopify.com/s/files/1/0924/6244/9007/files/CPKA_Machinery_nero_rgb-2048x240.jpg?v=1755761739" alt="COSMOPROF ASIA" loading="lazy" width="{{ am_right_logo_w }}" height="{{ am_right_logo_h }}">
            </div>
          </div>
        </div>
      </div>
    </div>
  <ul class="list-menu list-menu--inline" role="list">
    {%- for link in section.settings.menu.links -%}
      <li>
        {%- if link.links != blank -%}
          <header-menu>
            <details id="Details-HeaderMenu-{{ forloop.index }}" class="aseanmagic-menu">
              <summary
                id="HeaderMenu-{{ link.handle }}"
                class="header__menu-item list-menu__item link focus-inset"
              >
                <span
                  {%- if link.child_active %}
                    class="header__active-menu-item"
                  {% endif %}
                >
                  {{- link.title | escape -}}
                </span>
                {{- 'icon-caret.svg' | inline_asset_content -}}
              </summary>
              <div
                id="AseanmagicMenu-Content-{{ forloop.index }}"
                class="aseanmagic-menu__content color-{{ section.settings.menu_color_scheme }} gradient motion-reduce global-settings-popup"
                tabindex="-1"
              >
                <ul class="aseanmagic-menu__list" role="list">
                  {%- for childlink in link.links -%}
                    <li class="aseanmagic-menu__item">
                      <a
                        id="HeaderMenu-{{ link.handle }}-{{ childlink.handle }}"
                        href="{{ childlink.url }}"
                        class="aseanmagic-menu__link aseanmagic-menu__link--level-2 link{% if childlink.current %} aseanmagic-menu__link--active{% endif %}"
                        {% if childlink.current %}
                          aria-current="page"
                        {% endif %}
                      >
                        {{ childlink.title | escape }}
                      </a>
                      {%- if childlink.links != blank -%}
                        <ul class="aseanmagic-submenu" role="list">
                          {%- for grandchildlink in childlink.links -%}
                            <li>
                              <a
                                id="HeaderMenu-{{ link.handle }}-{{ childlink.handle }}-{{ grandchildlink.handle }}"
                                href="{{ grandchildlink.url }}"
                                class="aseanmagic-submenu__link link{% if grandchildlink.current %} aseanmagic-menu__link--active{% endif %}"
                                {% if grandchildlink.current %}
                                  aria-current="page"
                                {% endif %}
                              >
                                {{ grandchildlink.title | escape }}
                              </a>
                            </li>
                          {%- endfor -%}
                        </ul>
                      {%- endif -%}
                    </li>
                  {%- endfor -%}
                </ul>
              </div>
            </details>
          </header-menu>
        {%- else -%}
          <a
            id="HeaderMenu-{{ link.handle }}"
            href="{{ link.url }}"
            class="header__menu-item list-menu__item link link--text focus-inset"
            {% if link.current %}
              aria-current="page"
            {% endif %}
          >
            <span
              {%- if link.current %}
                class="header__active-menu-item"
              {% endif %}
            >
              {{- link.title | escape -}}
            </span>
          </a>
        {%- endif -%}
      </li>
    {%- endfor -%}
  </ul>
</nav>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    var mq = window.matchMedia('(min-width: 990px)');
    var isDesktop = function () { return mq.matches; };

    document.querySelectorAll('.aseanmagic-menu-desktop header-menu details.aseanmagic-menu').forEach(function (details) {
      var summary = details.querySelector('summary');
      var hoverTimer;

      var openOnHover = function () {
        if (!isDesktop()) return;
        clearTimeout(hoverTimer);
        details.setAttribute('open', '');
      };

      var closeOnLeave = function () {
        if (!isDesktop()) return;
        hoverTimer = setTimeout(function(){ details.removeAttribute('open'); }, 120);
      };

      details.addEventListener('mouseenter', openOnHover);
      details.addEventListener('mouseleave', closeOnLeave);

      if (summary) {
        summary.addEventListener('click', function (e) {
          if (!isDesktop()) return; // 移动端保留点击展开
          e.preventDefault(); // 桌面端阻止点击切换，用悬停控制
        });
      }
    });
  });
</script>
